name: Dependabot auto-merge
on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot-auto-merge:
    uses: maxwelldps/workflows/.github/workflows/dependabot-auto-merge.yml@main
    secrets: inherit
